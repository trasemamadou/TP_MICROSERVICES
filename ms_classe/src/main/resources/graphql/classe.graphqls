type Classe {
    id: ID!
    nom: String!
}

type Query {
    allClasses: [Classe!]!
    getClasseById(id: ID!): Classe
}

type Mutation {
    createClasse(nom: String! ): Classe!
    updateClasse(id: ID!, nom: String ): Classe!
    deleteClasse(id: ID!): String
}
